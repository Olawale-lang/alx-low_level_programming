#include <stdio.h>

/**
 * main - the main function serves as the entry point of a program
 *
 * Return: 0 when successfull
 */

int main(void)
{
	puts("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
